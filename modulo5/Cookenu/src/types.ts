export type recipeDB = {
    id: string
    title: string
    description: string
    cratedAt: Date
    recipe_id: string
}
export type UserCok = {
    id: string
    name: string
    email: string
    password: string
}