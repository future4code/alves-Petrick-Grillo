```
function calculaNota(ex, p1, p2) {
    somaPonderada = ex + p1 + p2
    mediaPonderada = somaPonderada / 3
    if (mediaA = mediaPonderada >= 9) {
        return("A")
    } else if ((mediaB = mediaPonderada < 9) && (mediaPonderada>= 7.5)) {
        return("B")
    } else if (mediaC = mediaPonderada < 7.5 && (mediaPonderada>= 6)) {
        return("C")
    } else
        return("D")
}
```